package leaderBoard;
import java.io.*;

public class HighScore {
	/*private static int highscore;
	public static void Score(int score){
		highscore = score;
		int[] Leaderboard = getLeaderboard();
		if(checkScore(Leaderboard, score)){
			newLeaderboard(Leaderboard);
		}
		
	}
	private static int[] getLeaderboard() throws {
		try{
			File file = new File("scores.FBGM");
			if (!file.exists()){
				file.createNewFile();
			}
			FileInputStream fileInput = new FileInputStream(file);
			ObjectInputStream input = new ObjectInputStream(fileInput);
			return (int[]) input.readObject();
		}catch (IOException ioexception){
			ioexception.getMessage();
		}
		return null;
	}
	private static boolean checkScore(int[] leaderboard, int score) {
		// TODO Auto-generated method stub
		return false;
	}
	private static void newLeaderboard(int[] leaderboard) {
		// TODO Auto-generated method stub
		
	}*/
}
